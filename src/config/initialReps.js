// Initial rep seed — applied once when the reps store is empty (first boot).
// IMPORTANT: Drew replaces XXXX locally with his actual last-4 SSN before
// first run. NEVER commit the real value — keep this file at 'XXXX' in the repo.
export const INITIAL_REPS = [
  {
    firstName: 'Drew',
    lastName: 'Yomantas',
    initials: 'DY',
    last4Ssn: 'XXXX',
    active: true,
  },
]
